# mocks/

Reusable mock factories for browser APIs used across tests.

## Files

| File | Description |
|------|-------------|
| `browserAPIs.ts` | `createMockAudio()` — returns a mock `HTMLAudioElement` with jest.fn() stubs for `play`, `pause`, `onended`, `src`, and `currentTime` |
